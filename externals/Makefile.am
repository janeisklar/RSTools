# let's not name it libfslio.la, so that it doesn't conflict with the library that comes with FSL
lib_LTLIBRARIES = librsfslio.la

librsfslio_la_SOURCES = fslio/fslio.c
librsfslio_la_LIBADD = @LIBOBJS@

pkginclude_HEADERS = fslio/fslio.h fslio/dbh.h